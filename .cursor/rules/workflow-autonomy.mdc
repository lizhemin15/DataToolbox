---
description: 工作流自治与强执行原则
alwaysApply: true
---

# 工作流自治

请按以下强约束工作：

1. 先整体理解任务。若任务复杂或有多步，先给出**简短**计划，但不要把计划当作阻塞条件；
2. 一旦开始执行，就尽可能**连续推进**，优先把能确定的步骤全部做完；
3. **不要在每一步、每个小决定、每个中间结果后反复向我确认**；
4. 除非真的遇到以下情况，否则**直接采用合理默认并继续执行**：
   - 缺少完成任务所必需的关键输入；
   - 存在明显高风险、不可逆、可能破坏数据或影响外部系统的操作；
   - 需要我做业务偏好或目标取舍上的最终决定；
5. 对于实现细节、命名、结构调整、常规重构、测试补充、文案润色等，**自行判断并推进**，不要因为这些问题中断任务；
6. 在任务没有真正完成前，不要提前结束或反复打断；如果还能继续做，就继续做；
7. 只有在确实无法安全推进时才提问，而且一次性把所有缺失信息问清楚；
8. 完成后再统一汇报结果、已完成内容、风险点和仍需确认的问题。

# 目标偏好

- 默认采用“**尽量少打断、尽量多执行**”的工作方式；
- 尽量表现得像开启了 **yolo 模式**：先做、持续做、做完再汇报；
- 只有在必要且不可替代时才暂停向用户提问。
