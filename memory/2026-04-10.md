# 2026-04-10 工作日志

## DataToolbox 项目 - 继续完善

### 完成的工作
1. **用户管理 UI 位置调整** - 从左侧数据库管理下移到右上角用户名旁 ⚙️ 按钮，改为右侧抽屉面板
2. **API Key 生成 Bug 修复** - 旧版本二进制没有 admin 为其他用户生成 key 的逻辑，部署新版本后修复
3. **登录过期自动跳转** - 统一 `fetchWithAuth` 函数处理 401，自动清除 token 跳转登录页，登录后恢复之前页面
4. **密码二次确认** - 创建用户和修改密码都需要输入两次密码，不一致提示，密码至少4位

### 数据治理模块现状
- ✅ 读取 Word/Excel — `gov.readWord()`、`gov.readExcel()`
- ❌ 生成/导出 Word/Excel — 未实现
- 前端有 XLSX 库（SheetJS），可用 `XLSX.writeFile()` 生成 Excel，但未封装
- 用户问是否支持生成 word/excel，待确认需求

### Commits 顺序
- `2ff9b4e` fix: 用户管理移到右上角，改为右侧抽屉面板
- `1b01b12` fix: 用户管理API Key - 未生成时显示生成按钮，已生成可复制
- `10cd215` debug: 添加apikey生成日志
- `8ae2465` fix: 登录过期自动跳转登录页，统一 fetchWithAuth 处理 401
- `78fc4dd` fix: 创建用户和修改密码加入二次确认，密码至少4位

### 部署流程备忘
- 前端文件更新后手动 cp 到运行目录
- data-store.json 重启前备份恢复
- 后端改动需等 GitHub Action 编译后下载部署
