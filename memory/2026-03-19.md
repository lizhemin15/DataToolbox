# 2026-03-19 记忆

## 已完成任务

- **修复飞书语音消息重复回复问题**：已完成
  - 问题：语音消息会收到两次回复（一个正常识别，一个没识别到）
  - 解决方案：添加消息去重机制，使用 processedMsg map 记录已处理的消息 ID
  - 添加 processedRing 实现 LRU 淘汰，限制缓存大小
  - 代码已提交推送，容器已重启部署

1. **语音 API 桥接路由实现**（已完成）
   - 后端：VoiceEndpoint 配置结构、语音桥接 handler
   - 路由：/asr/v1/audio/transcriptions、/tts/v1/audio/speech
   - 前端：语音端点 CRUD API、配置 UI
   - 支持负载均衡、故障转移、429/5xx 自动冷却
   - 已部署完成

2. **语音配置 UI 简化**（已完成）
   - 移除 token 和 QPS 限制字段
   - 保持：name、endpoint、api_key、type、models、default_voice、enabled
   - 已部署完成

## 效率优化

- 创建了 anyclaw-dev skill 的快速脚本：
  - `smart-deploy.sh` — **推荐**：智能一键部署，最小化汇报，避免消息轰炸
  - `smart-fix.sh` — 智能修复，带进度监控，每30秒汇报一次
  - `quick-fix-deploy.sh` — 一键修复并部署
  - `fix-issue.sh` — 仅修复代码
  - `deploy.sh` — 仅部署
  - `check-status.sh` — 检查状态
- **使用建议**：优先使用 `smart-deploy.sh`，只输出关键进度，不会频繁发消息

- 项目路径: `/root/projects/anyclaw`
- 部署命名空间: `ns-h2c2nyvr`
- StatefulSet: `anyclaw`
- 公网地址: `https://htwkumkjgrnz.sealosbja.site`
- **用户偏好**: 改代码时要主动汇报进度，5分钟一次，不要让用户干等
- **效率优化**: 使用 anyclaw-dev skill 的脚本快速修复部署
