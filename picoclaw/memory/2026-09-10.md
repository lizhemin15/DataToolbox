# 2026-09-10

## 任务:DataToolbox 智能助手「未在已修改文件基础上继续修改」bug

**用户反馈(原话)**: "就是先让它填了一个word,它发过来了,我继续提需求,它就没理解对,没有在修改的文件的基础上继续修改"

**排查进展(已完成代码层面核查)**:
- DataToolbox 智能助手(PicoClaw 集群)工具集 = mcp.go + agent/datatoolbox_tool.go。
- ✅ 确认:mcp.go + datatoolbox_tool.go **没有任何"执行治理任务生成/填充 Word"的工具**。
  - mcp.go 工具:list_databases/get_tables/describe_table/profile_table/execute_sql/list_apis/get_api_detail/call_api/search_tables/get_db_schema/get_db_sql_hints/create_api/execute_api/list_platforms/get_platform_detail/list_platform_apis/call_platform_api/create_app/create_dashboard/list_templates/create_app_from_template/list_apps/update_app/delete_app/ask_user。
  - datatoolbox_tool.go 工具:governance_tasks(只读列表)/get_governance_task(只读详情) + 查询/接口/本体。
- 生成 Word 的能力在「治理任务」模块(executeGovernanceTaskForAPI → 落盘 apps/data-ontology/api-outputs/{taskID}/{runID}/ → 返回 output_files 下载链接)。
- PicoClaw 多轮会话:orchestrator_pico.go → loop.ProcessDirectWithChannel → agent_message.go,EnableSummary=true,按 sessionKey 加载历史。

**根因判断**:
- 智能助手工具集本身无"生成 Word"工具 → "填 Word"动作的真实入口待确认(治理任务界面?智能助手调治理任务注册API?)。
- 多轮对话中第一轮生成的 Word 文件**内容/路径未带入第二轮 LLM 上下文** → 第二轮 LLM 看不到已填内容 → 无法"基于已修改文件继续修改"→ 重新生成/理解成新任务。

**阻塞点**: 需用户确认「填 Word」发生的具体入口(否则可能修错模块)。

**状态**: 排查中 ❌,等待确认入口